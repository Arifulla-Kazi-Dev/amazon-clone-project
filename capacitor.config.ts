import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amazonclone.projectblue',
  appName: 'NovaCart',
  webDir: 'dist/amazon-clone/browser'
};

export default config;
